namespace Contacts {
    public class testmain {
        public static void main (string args[]) {
            var decoder = new Decoder (stdin.read_line ());
            print (decoder.output);
        }
    }
}
